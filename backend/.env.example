#
# Backend environment variables (example)
#
# Copy to `backend/.env` for local dev, then restart the backend.
# Never commit real secrets.
#

# --- OTP configuration ---
# OTP_EXP_MINUTES=10

# --- Email configuration ---
# Recommended: Brevo transactional email
# EMAIL_BACKEND=brevo
# BREVO_API_KEY=your_brevo_api_key_here
# BREVO_FROM=info@srtech.co.in
# BREVO_SENDER_NAME=SRTech

# Easiest (dev/staging): log OTP/email contents to server logs
# EMAIL_BACKEND=console

# Alternative: SMTP (if you prefer)
# EMAIL_BACKEND=smtp
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_smtp_username
# SMTP_PASS=your_smtp_password
# SMTP_FROM=info@srtech.co.in

