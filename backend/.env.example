## Example environment variables for backend (do not commit real secrets).
##
## Google Sign-In:
## Allow both the Web and Android OAuth client IDs as valid audiences.
GOOGLE_OAUTH_CLIENT_IDS=333176294914-nusbltfj219k3ou30dnqluvcqsvsr93d.apps.googleusercontent.com,333176294914-t7b1h2ams20nn0dvf2k4n5cedq71q8dm.apps.googleusercontent.com

#
# Backend environment variables (example)
#
# Copy to `backend/.env` for local dev, then restart the backend.
# Never commit real secrets.
#

# --- OTP configuration ---
# OTP_EXP_MINUTES=10

# --- App environment ---
# APP_ENV=local|staging|prod
#
# IMPORTANT: In production you must set:
# - DATABASE_URL
# - JWT_SECRET
# - ALLOWED_HOSTS

# Example:
# APP_ENV=prod
# JWT_SECRET=super-long-random-secret
# ALLOWED_HOSTS=api.yourdomain.com

# --- Email configuration ---
# Recommended: Brevo transactional email
# EMAIL_BACKEND=brevo
# BREVO_API_KEY=your_brevo_api_key_here
# BREVO_FROM=info@srtech.co.in
# BREVO_SENDER_NAME=SRTech

# Easiest (dev/staging): log OTP/email contents to server logs
# EMAIL_BACKEND=console

# Alternative: SMTP (if you prefer)
# EMAIL_BACKEND=smtp
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_smtp_username
# SMTP_PASS=your_smtp_password
# SMTP_FROM=info@srtech.co.in

# --- Google Play subscription validation ---
# GOOGLE_PLAY_PACKAGE_NAME=com.yourcompany.yourapp
# GOOGLE_PLAY_SERVICE_ACCOUNT_FILE=google_service_account.json

