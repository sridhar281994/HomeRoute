services:
  - type: web
    name: homeroute-api
    env: python
    plan: free
    buildCommand: pip install -r backend/requirements.txt
    startCommand: bash -lc "cd backend && alembic -c alembic.ini upgrade head && cd .. && uvicorn main:app --host 0.0.0.0 --port $PORT"
    autoDeploy: true
